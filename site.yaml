---
- add_host:
    name: "{{ item.dns_name }}"
    groups: ec2-test
  with_items: ec2.instances
  tasks:
    - name: ensure nginx is at the latest version
      apt: name=nginx state=latest
    - name: start nginx
      service:
          name: nginx
          state: started
